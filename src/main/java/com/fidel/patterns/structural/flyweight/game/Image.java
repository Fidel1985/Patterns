package com.fidel.patterns.structural.flyweight.game;

public class Image {

    public static Image load(String name) {
        //image loading from disk by file name
        return new Image();
    }
}
